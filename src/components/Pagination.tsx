import React from "react";
import { Pagination } from "semantic-ui-react";

const PaginationComponent = () => <Pagination defaultActivePage={5} totalPages={10} />;

export default PaginationComponent;
