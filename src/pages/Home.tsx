import React from "react";
import Layout from "../components/layout/index";

const Home = () => {
    return (
        <Layout>
            <div>HOME</div>
        </Layout>
    );
};

export default Home;
